import type { Metadata } from "next";

export const metadata:Metadata = {
 title: 'Contact Paage',
 description: 'Contact Paage',
};
export default function ContactPage() {
    return (
      <div>
        <span className="text-7xl">Contact</span>
      </div>
    );
  }